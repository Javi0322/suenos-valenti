<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Perfil - Sueños Valenti</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body class="<%= tema %> perfil">
    <h1>Zona privada / Perfil</h1>

    <p class="card"><strong>ID:</strong> <%= usuario.id %></p>
    <p class="card"><strong>Nombre:</strong> <%= usuario.nombre %></p>
    <p class="card"><strong>Email:</strong> <%= usuario.email %></p>
    <p class="card"><strong>Edad:</strong> <%= usuario.edad %></p>
    <p class="card"><strong>Ciudad:</strong> <%= usuario.ciudad %></p>

    <h3>Intereses</h3>
    <ul>
      <% (usuario.intereses || []).forEach(i => { %>
      <li><%= i %></li>
      <% }) %>
    </ul>

    <form method="POST" action="/logout">
      <button type="submit">Cerrar sesión</button>
    </form>

    <p><a href="/">Volver al inicio</a></p>
  </body>
</html>
